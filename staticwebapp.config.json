{
    "routes": [
        {
            "route": "/robots.txt",
            "serve": "/robots.txt"
        },
        {
            "route": "/*",
            "serve": "/index.html"
        }
    ],
    "navigationFallback": {
        "rewrite": "index.html",
        "exclude": ["/assets/*", "*.js", "/robots.txt"]
    }
}